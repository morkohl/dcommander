const arguments = [
    {
        argument: {
            argumentName: 's',
            prefix: '--',
            type: types.string,
            validate: [],
            sanitize: [],
            required: false
        }
    }
];

export class Utils {
    static compile()
}